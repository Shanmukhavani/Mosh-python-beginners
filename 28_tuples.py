#tuples are immutable , so we dont have append , insert methods in tuples.if we try to modify you will get error.
numbers = (1,2,3)
#numbers[0]=5 --->gives error
#we only have index() and count() methods in tuples.